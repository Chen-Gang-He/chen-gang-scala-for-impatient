package com.chengang.ch11

/**
  * The BigInt class has a pow method, not an operator. Why didn’t the Scala library designers choose ** (as in Fortran)
  * or ^ (as in Pascal) for a power operator?
  **/

object Ex2 extends App {
  // Precedent problem, power must have highest precedence.

}
